name: build

on:
  push:
    branches: ["main"]
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: [self-hosted]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Build website
        run: |
          julia --project=. -e 'using ShinGroupIO; ShinGroupIO.build()'
